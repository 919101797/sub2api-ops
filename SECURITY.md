# 安全政策

## 报告漏洞

请使用 [GitHub 私密漏洞报告](https://github.com/919101797/sub2api-ops/security/advisories/new)，不要先发布公开 Issue。报告可包含受影响提交或版本、最小复现、影响范围与修复建议；使用虚构数据，不附真实凭据或用户提示词。

维护者会根据影响评估处理顺序，修复后协调披露。当前维护范围是默认分支；没有承诺旧版本长期支持或固定响应时限。

## 发现已泄漏凭据

先在签发服务撤销或轮换凭据，再清理文件和历史。删除最新文件不能撤销已泄漏密钥，也不能自动移除 Git 历史。不要把泄漏值复制到报告中；提供文件路径、提交号和字段名称即可。

## 部署边界

- Ops 具有管理上游额度、用户限流与解封的能力，使用专用凭据并限制管理入口。
- 生产配置使用 HTTPS、Secure Cookie 和准确的 `PUBLIC_ORIGIN`；内部采集端点由共享密钥保护，不直接暴露到公网。
- 数据库读写角色分离；应用启动不使用数据库超级用户。权限配置见[部署文档](docs/operations/deployment.md)。
- 会话密钥、采集密钥、管理员 Key 及数据库密码只进入部署环境，不写进浏览器配置、镜像或源码。
- `data/`、发件箱、审计数据库与媒体可能包含用户内容。配置适当留存、权限和备份访问控制；内置文本脱敏不能保证识别所有敏感信息。

`.gitignore` 用于减少误提交，不能代替提交内容检查。依赖变更执行全量检查；生产服务器只运行已构建镜像。
