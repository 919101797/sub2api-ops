# 贡献指南

欢迎提交可复现的问题、改进文档和功能变更。开始前阅读[README](README.md)、[开发指南](docs/development/setup.md)和[行为准则](CODE_OF_CONDUCT.md)。

## 提交问题

在 [Issues](https://github.com/919101797/sub2api-ops/issues) 描述预期与实际行为、最小复现步骤、Ops 提交或版本、Node.js / 浏览器 / Sub2API 版本，以及已脱敏的错误信息。功能建议请描述使用场景和期望结果。

安全漏洞和凭据泄漏使用[安全政策](SECURITY.md)中的私密渠道。不要在公开 Issue、PR、截图和日志中附上真实 `.env`、数据库导出、Cookie、邮箱、API Key 或用户审计正文。

## 本地工作

Fork 仓库后创建一个范围明确的分支。按[开发指南](docs/development/setup.md)安装依赖；测试与构建不需要生产环境。优先使用现有依赖和组件，保持后端、前端及共享契约的职责边界。

行为变化先修改对应[功能规格](docs/specs/README.md)，写出用户可以观察到的场景。测试跟随源码放置，用最小相关测试验证，再按[全量触发条件](docs/development/testing.md)扩大检查。不要新增旧接口兼容层、重复配置体系或不可执行的 `.feature` 文件。

新依赖或素材需提供来源、许可证和必要性，更新锁文件与第三方声明。对现有依赖的版本升级也应单独说明影响，避免混入不相关 UI 修改。

## 发起 Pull Request

描述问题、最终行为和测试结果；涉及界面时附使用虚构数据的截图或短视频，涉及部署时同步更新操作文档。命令未执行或需要外部服务验证时如实注明，不把模拟测试描述为生产验收。

通常执行：

```bash
npm run typecheck
npm test
npm run build
```

纯文档修改检查路径、命令和配置示例是否与源码一致。不要提交 `node_modules/`、`dist/`、`.env`、`config.json`、`data/` 或日志。提交信息说明实际变化即可；不要求固定格式。

发生合并冲突时保留双方预期行为，重新检查最终差异并执行受影响测试。不要直接整文件接受某一侧来消除冲突。

## 许可

你应有权提交所贡献的内容。原创贡献按项目 [MIT 许可证](LICENSE)提供；第三方代码保留其原许可和来源，具体见[第三方声明](THIRD_PARTY_NOTICES.md)。
